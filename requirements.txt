Flask>=3.0.3
Flask-SQLAlchemy>=3.1.1
Flask-Cors>=5.0.0
SQLAlchemy>=2.0.43
Werkzeug>=3.0.4
gunicorn>=23.0.0
qrcode>=7.4.2
pytest>=8.3.3

# Driver PostgreSQL para produção (Neon/Supabase/Fly Postgres).
# Em dev local com SQLite, este pacote é instalado mas não usado.
psycopg[binary]>=3.2.0

# Sprint 2 — segurança
Flask-JWT-Extended>=4.6.0
Flask-Limiter>=3.8.0

# Google OAuth — validação de ID tokens vindos do site e do app iOS/Mac.
# google.auth.transport.requests EXIGE a lib `requests` instalada
# (não é dependência transitiva forçada pelo google-auth). Por isso pinamos
# ambas explicitamente. Sem o `requests`, o endpoint /auth/google retorna
# 500 "dependência google-auth não instalada" (engano: na verdade é requests).
google-auth>=2.30.0
requests>=2.32.0

# Onda 2 — Auth refactor completo
argon2-cffi>=23.1.0       # hashing Argon2id (substitui bcrypt; mantém compat)
pyotp>=2.9.0              # TOTP (RFC 6238) — MFA
qrcode[pil]>=7.4.2        # QR code do TOTP setup
phonenumbers>=8.13.0      # validação + normalização E.164 (celular)
Flask-Talisman>=1.1.0     # CSP, HSTS, X-Frame-Options, Referrer-Policy
